tables:
  - schema: public
    table: identity_credential_types
    output_table_name: user_kratos_identity_credential_types
    enabled: true
    select_sql: |
      id::text as id,
      name
    output_columns:
      - id
      - name