# postgres-dlt-s3-snapshots
Extract full and incremental (delta) snapshots from PostgreSQL tables and write them to S3 as Parquet using dlt, with YAML-driven configuration and optional filtering by database/table.
